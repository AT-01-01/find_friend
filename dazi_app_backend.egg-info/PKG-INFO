Metadata-Version: 2.4
Name: dazi-app-backend
Version: 0.1.0
Summary: FastAPI backend skeleton for Dazi matching app
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: fastapi
Requires-Dist: uvicorn[standard]
Requires-Dist: sqlalchemy>=2.0
Requires-Dist: psycopg[binary]
Requires-Dist: pydantic-settings
Requires-Dist: email-validator
Provides-Extra: dev
Requires-Dist: pytest; extra == "dev"
Requires-Dist: httpx; extra == "dev"

# dazi_app_backend

`dazi_app_backend` 是一个基于 FastAPI + SQLAlchemy + PostgreSQL 的“搭子/匹配”后端骨架工程，包含用户、匹配、活动、聊天会话等基础能力。

## 快速启动

1. 创建并激活虚拟环境
2. 安装依赖  
   `pip install -e .[dev]`
3. 复制环境变量模板并按需修改  
   将 `.env.example` 复制为 `.env`
4. 启动服务  
   `uvicorn app.main:app --reload`

启动后可访问：
- 健康检查：`GET /health`
- API 前缀：`/api/v1`

## 核心目录结构

```text
app/
  api/         # 路由层：对外 HTTP 接口
  core/        # 配置层：环境变量与全局设置
  db/          # 数据库层：引擎、会话、ORM Base
  models/      # 数据模型层：SQLAlchemy ORM 模型
  schemas/     # 协议层：Pydantic 请求/响应模型
  services/    # 业务层：核心业务逻辑与数据库读写
tests/         # 测试
```

## 包与文件职责说明

### 入口与配置

- `app/main.py`：应用入口，创建 FastAPI 实例，挂载 `/api/v1` 路由；启动时按 `AUTO_CREATE_TABLES` 自动建表；提供 `/health` 健康检查。
- `app/core/config.py`：`Settings` 配置类，读取 `.env` 中的 `APP_NAME`、`APP_ENV`、`DATABASE_URL`、`AUTO_CREATE_TABLES`。
- `.env.example`：本地开发环境变量模板。
- `pyproject.toml`：项目元数据、依赖定义、pytest 配置。

### API 路由层（`app/api`）

- `app/api/__init__.py`：导出顶层 `api_router`。
- `app/api/v1/router.py`：v1 路由聚合，注册 users/match/activities/chats 四类子路由。
- `app/api/v1/endpoints/user.py`：用户接口（注册、登录、按 ID 查询）。
- `app/api/v1/endpoints/match.py`：匹配接口（按城市/兴趣关键字搜索用户）。
- `app/api/v1/endpoints/activity.py`：活动接口（发布活动、活动列表）。
- `app/api/v1/endpoints/chat.py`：聊天会话接口（创建会话、查询用户会话列表）。

### 业务层（`app/services`）

- `app/services/user_service.py`：用户创建、登录校验、按 ID 查询。  
  当前密码逻辑为占位实现（明文比较），后续应替换为安全哈希。
- `app/services/match_service.py`：匹配查询逻辑，支持 `city` 与 `interest_keyword` 条件过滤。
- `app/services/activity_service.py`：活动发布与活动倒序列表查询。
- `app/services/chat_service.py`：聊天会话创建、按用户查询关联会话。

### 数据模型层（`app/models`）

- `app/models/user.py`：`users` 表模型（用户名、邮箱、密码、城市、兴趣、简介）。
- `app/models/activity.py`：`activity_posts` 表模型（作者、标题、内容、城市）。
- `app/models/chat.py`：`chat_sessions` 表模型（双用户会话关系）。
- `app/models/__init__.py`：统一导出模型，便于集中导入触发表结构注册。

### 数据库基础层（`app/db`）

- `app/db/base.py`：ORM `Base` 声明（所有模型的基类）。
- `app/db/session.py`：数据库引擎、`SessionLocal`、请求级 `get_db()` 依赖注入。

### 请求/响应 Schema 层（`app/schemas`）

- `app/schemas/user.py`：用户注册、登录、返回结构定义。
- `app/schemas/match.py`：匹配查询参数与匹配结果结构定义。
- `app/schemas/activity.py`：活动创建与活动返回结构定义。
- `app/schemas/chat.py`：聊天会话创建与会话返回结构定义。

### 测试

- `tests/test_health.py`：健康检查基础测试，验证 `/health` 返回 `status=ok`。
